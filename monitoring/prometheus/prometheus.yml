global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  # FlowForge API runs on the host (uvicorn). Docker Desktop Mac: host.docker.internal
  - job_name: flowforge
    metrics_path: /metrics
    static_configs:
      - targets: ["host.docker.internal:8000"]
